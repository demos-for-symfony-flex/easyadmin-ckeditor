{
    "name": "symfony-flex-demo/easyadmin",
    "type": "project",
    "license": "MIT",
    "description": "A minimal EasyAdmin FOSCKEditor Flex project",
    "require": {
    },
    "require-dev": {
    },
    "bin": ["bin/console"],
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "auto-scripts": [
        ],
        "initialize": [
            "composer console doctrine:database:create",
            "doctrine:migrations:diff --quiet": "symfony-cmd",
            "doctrine:migrations:migrate --no-interaction --quiet": "symfony-cmd",
            "ckeditor:install --quiet": "symfony-cmd",
            "assets:install --symlink --relative": "symfony-cmd"
        ],
        "post-install-cmd": [
            "@auto-scripts"
        ],
        "post-update-cmd": [
            "@auto-scripts"
        ]
    },
    "conflict": {
        "symfony/symfony": "*"
    }
}
